# Winlator 优化练习项目工作报告

> 编制日期：2026-09-01
> 工作仓库：`D:\study\Winlator\winlator_wzh`
> 对照仓库：`D:\study\Winlator\winlator`
> 功能基线：`624ded8`（本报告编写前）
> 上游初始快照：`5acd6bc`，对应 Winlator app 11.1 / upstream `e113da42`
> 当前分支：`main`，远程：`origin/main`

## 1. 报告摘要

本练习围绕开源项目 Winlator 开展了三个阶段的优化：桌面交互功能开发、核心运行组件升级及架构梳理、多游戏兼容性与性能问题分析。截至本报告编写时，项目1和项目2已完成；项目3已完成 Dave the Diver 启动兼容修复、内置 rootfs 集成、跨游戏键盘输入修复、HUD 鼠标回归和 4/5 个目标样本的有效测试，性能基线与 Sifu 有效首轮仍未完成，现按用户要求暂停。

| 工作域 | 当前状态 | 核心结果 |
|---|---|---|
| 项目1：桌面关机悬浮球 | 已完成 | 可拖动、位置持久化、点击确认、复用标准退出链路 |
| 项目2：组件升级与架构 | 已完成 | Box64、DXVK、VKD3D、Turnip、Wine 11 升级；架构及调用链完成梳理 |
| 项目3：兼容性问题解决 | 已完成主要闭环 | Dave 两次冷启动及 6 分钟稳定性通过；输入与鼠标回归通过 |
| 项目3任务1：多游戏覆盖 | 进行中，当前暂停 | 5 个目标样本中 4 个已有有效结果；Sifu 尚无 Shipping 进程证据 |
| 项目3性能优化 | 工具已实现，基线未完成 | CSV 采样器已加入，但尚未捕获真实持续帧更新，不能形成性能结论 |
| 研发证据与归档 | 已建立持续机制 | 构建、运行、输入、图形测试、哈希、清单和 Git 节点均可追溯 |

## 2. 原始任务与执行范围

根目录 `练习项目.docx` 对三个阶段的要求为：

1. 编译运行 Winlator，体验产品并增加桌面关机悬浮球。
2. 编译 Box64、Wine、DXVK、VKD3D、Turnip 等组件，尝试升级到新版本；整理编译问题、技术架构、渲染链路和二进制翻译链路。
3. 测试多款游戏，分析并解决一个兼容性问题，分析低帧率游戏并实现渲染转换效率优化方案。

执行过程中增加了以下工程边界：

- 商业游戏本体、修改包和游戏压缩包不进入 Git；仓库只保存清单、指纹、日志和研究结论。
- 每轮实验只改变一个兼容变量，保留失败基线与成功对照，避免多变量同时变化造成错误归因。
- 运行、输入、性能、构建日志使用唯一文件名和 SHA-256，不覆盖历史样本。
- 用户可见的“黑屏”“闪退”不能直接作为根因，必须结合实际 EXE、容器、进程、图形初始化和退出证据分类。
- 早期假设在后续被反证时，以最终证据更新结论，不用旧结论替代完整时间线。

## 3. 仓库基线与总体改动

原版仓库当前以子模块方式组织，`app`、`gladio`、`vortek` 在本地未展开，因此逐文件代码对照采用练习仓库初始提交 `5acd6bc` 作为稳定上游基线，同时保留原版顶层提交与子模块指针信息。

截至功能基线 `624ded8`：

- 相对初始提交共有 83 个后续提交。
- 仓库约 1,383 个 tracked 文件。
- 排除 `docs/` 与 `archive/` 后，源码、资源、构建配置和脚本涉及 55 个路径，文本约新增 1,557 行、删除 133 行。
- 项目3相对项目2最终节点 `1d4585d` 涉及 27 个非文档/非归档路径，约新增 1,002 行、删除 47 行。
- 当前主应用为 `com.winlator`，版本 `11.1` / `versionCode 28`，`minSdk 26`、`targetSdk 28`、`compileSdk 34`，Debug 构建仅包含 `arm64-v8a`。
- Android 工具链固定 NDK `24.0.8215888`、CMake `3.22.1`、AGP `7.2.2`、Gradle `7.3.3`，实际构建使用 JDK/JBR 17。

主要新增或强化的代码域包括：

- 桌面悬浮球和退出交互。
- 组件资产版本更新、Wine 安装器、prefix 校验与 rootfs 迁移。
- 启动阶段日志、Box64 解压自愈与错误收口。
- X11 焦点事件、键盘映射、Wine 输入跟踪及 x86_64 XKB fallback。
- FPS/帧时间 CSV 采样和 HUD 触摸穿透修复。
- 游戏清单、开发日志增量归档和敏感信息/大文件门禁脚本。

## 4. Winlator 技术架构梳理

### 4.1 应用与 guest 启动链

本 fork 不使用 proot/chroot。`rootfs.tzst` 解压到 Android 应用私有目录，Java 通过 `ProcessBuilder` 直接启动原生子进程，并使用绝对路径、`PATH`、`LD_LIBRARY_PATH`、`BOX64_LD_LIBRARY_PATH` 等环境变量提供运行环境。

```text
Android Activity / Java
  -> XEnvironment 按顺序启动 IPC 与渲染组件
  -> GuestProgramLauncherComponent
  -> ARM64 Box64
  -> x86_64 Wine
  -> Windows EXE
```

环境组件的关键顺序为：

```text
SysVSharedMemory
  -> XServer
  -> NetworkInfo
  -> ALSA / PulseAudio
  -> Vortek / VirGL
  -> GuestProgramLauncher
```

IPC 服务端先监听，最后才启动 guest，避免 Wine/游戏连接到尚未准备好的 X11、音频或渲染服务。

### 4.2 容器与 rootfs

- rootfs 位于应用 `files/rootfs`，主 Wine 路径固定为 `/opt/wine`。
- 每个容器保存于 `rootfs/home/xuser-<id>`，`.container` 保存配置。
- 激活容器时，`home/xuser` 指向对应容器目录；`.wine/drive_c` 是 Windows C 盘。
- `container_pattern.tzst` 提供 prefix 模板，`common_dlls.json` 决定需要从 Wine 目录注入的公共 DLL。
- 当前 `RootFSInstaller.LATEST_VERSION=23`，主 Wine 迁移门槛为 `UPDATE_WINEPREFIX_VERSION=21`。

### 4.3 二进制翻译链

Box64 负责把 x86_64 Linux/Wine 指令动态翻译为 ARM64。Winlator 支持 `Stability`、`Conservative`、`Intermediate`、`Performance` 等预设，并通过 `BOX64_DYNAREC_*` 控制翻译块、内存序、标志位和浮点策略；per-exe `.box64rc` 可进一步覆盖单程序行为。

当前默认 Box64 为 `0.4.5-dev-372739d`，`0.4.3` 作为已验证回退版本。项目3对 Dave 的最终配置使用 `Intermediate` 和 `BOX64_UNITYPLAYER=1`。

### 4.4 图形渲染链

```text
D3D9/10/11 -> DXVK ------┐
D3D12       -> VKD3D ----+-> Vulkan ICD -> Vortek/Turnip -> 实际 Vulkan/GPU
WineD3D     -> OpenGL ---+-> Zink/VirGL/GLX
```

Vortek guest 驱动通过 Unix socket 交换共享内存 FD，主要命令负载走 ashmem 环形缓冲；host 侧反序列化 Vulkan 调用并调用实际驱动。窗口渲染结果使用 `AHardwareBuffer`，在 host 侧同时表现为 Vulkan `VkImage` 和 GLES 纹理，最后由 `GLRenderer` 合成到 `XServerView`。

主要 IPC 路径包括 `/tmp/.X11-unix/X0`、`.vortek/V0`、`.virgl/V0`、SysV SHM、音频 socket，以及 WinHandler UDP loopback 控制通道。

### 4.5 组件资产模型

Box64、Wine、DXVK、VKD3D、Turnip 并非由应用 Gradle 构建现场编译。它们分别在上游源码树中编译，再按 Winlator 既有结构封装为 `.tzst` 或 Wine addon，进入 `app/src/main/assets/`。因此组件升级必须同时验证：

1. 目标 ABI/libc 与 rootfs 一致。
2. 32/64 位目录和 DLL/ELF 类型正确。
3. 动态依赖在 rootfs 中齐全。
4. 文件权限、符号链接和打包路径正确。
5. 版本常量能触发手机端资产刷新。
6. 最终 APK 中的资产与源码资产哈希一致。

## 5. 项目1：桌面可拖动关机悬浮球

### 5.1 完成内容

在容器桌面最上层增加 56dp 半透明红色圆形“关机”悬浮球，实现：

- 自由拖动与父容器边界钳制。
- 使用 `SharedPreferences` 保存位置，重进容器后恢复。
- 以 8dp 位移阈值区分点击和拖动。
- 点击后显示二次确认，避免误操作造成未保存进度丢失。
- 确认后结束当前 Winlator 虚拟环境并返回主界面。

### 5.2 技术实现

新增 `ShutdownBallView`，复用现有 `MagnifierView` 的拖动模式。在 `XServerDisplayActivity.setupUI()` 末尾加入该 View，使其位于 `XServerView`、`TouchpadView`、输入控件和 HUD 之上。

关机逻辑不自行复制底层流程，而是复用 `XServerDisplayActivity.exit()`：

1. `winHandler.stop()` 停止控制 socket 和 MIDI。
2. `environment.stopEnvironmentComponents()` 逆序停止 GuestProgramLauncher、XServer、音频等组件，清理 Wine/Box64 guest。
3. `AppUtils.restartApplication()` 结束旧 Android 进程并返回应用入口。

新增资源包括 `shutdown_ball_view.xml`、`shutdown_ball_background.xml` 和关机提示字符串。

### 5.3 主要卡点

| 卡点 | 处理方式 | 结果 |
|---|---|---|
| 点击与拖动手势冲突 | 完整跟踪触摸序列，以 8dp 阈值区分行为 | 避免拖动结束误触关机 |
| 简单关闭 Activity 可能遗留 guest | 复用原有统一 `exit()` 生命周期 | Wine、Box64、XServer、音频按标准链路停止 |
| `jniLibs` 与 CMake 生成 `.so` 重名 | `packagingOptions.jniLibs.pickFirsts += ['**/*.so']` | Debug APK 可打包；后续可进一步收窄匹配范围 |

### 5.4 验证与证据边界

功能提交为 `a217bdb`，涉及 7 个文件、新增 244 行。构建使用 JDK 17 和 `assembleDebug`。后续 Wine 11 真机记录确认桌面、WFM、鼠标和悬浮关机按钮长期正常。

项目1没有独立保存 APK SHA-256 或专属真机日志快照，因此严谨表述为“功能、构建说明和后续跨版本回归已完成”，不宣称存在项目1专属日志归档。

## 6. 项目2：组件升级与兼容攻坚

### 6.1 最终组件基线

| 组件 | 初始版本 | 当前版本/策略 | 验证状态 |
|---|---|---|---|
| Box64 | 0.4.0 | 默认 `0.4.5-dev-372739d`，保留 `0.4.3` | 0.4.3、0.4.5 均有真机通过记录 |
| DXVK | 2.4.1 | `3.0.2`，保留 `1.10.3` | 32/64 位构建集成；后续游戏及图形测试使用 |
| VKD3D-Proton | 2.14.1 | `3.0.1` | 32/64 位构建集成；D3D12 探针通过 |
| Turnip | 26.1.0 | `26.1.5` | 补齐依赖后 D3D9/11/12 探针通过 |
| Wine | 10.10-custom | 正式 `11.0` + `nsiproxy` NULL 保护；保留 10.10 | Box64 0.4.3/0.4.5 下真机游戏通过 |
| Vortek / Zink / VirGL / Gladio | 既有版本 | 2.1 / 22.2.5 / 23.1.9 / 1.0 | 保持；Vortek/Gladio 无公开源码可独立升级 |

### 6.2 Box64：从 ABI 错配到打包破坏

第一版按 Android NDK/Bionic 目标编译，APK 可安装但容器立即闪退：

```text
libc.so has bad ELF magic: 2f2a2047
```

`2f2a2047` 是文本 `/* G` 的十六进制开头，说明 Bionic Box64 在 glibc rootfs 中把 GNU ld script 当作 ELF 加载。对比 `NEEDED` 和 interpreter 后，构建目标改为 `ANDROID_GLIBC + aarch64-linux-gnu-gcc`。

修正 ABI 后，静态检查全部通过，真机却仍在 `main()` 前 exit 139，甚至 `BOX64_LOG/SHOWSEGV/SHOWBT` 无输出。早期曾怀疑设备固定内存布局，该结论随后被推翻。与可运行官方产物逐项差分后发现，自定义打包额外执行了 `patchelf --set-rpath` 和 `strip`。Box64 是非 PIE、固定加载地址约 `0x34800000` 的特殊二进制，事后改 rpath 会重写 program headers/`.dynamic` 并破坏加载。

最终配方：使用 glibc 交叉编译，只设置 rootfs interpreter，不设置 rpath、不 strip。`0.4.3-4` 真机正常进入桌面并启动星露谷；后续 0.4.5-dev 也通过并成为默认版本。

### 6.3 DXVK：Windows DLL 工具链必须自洽

DXVK 3.0.2 需要同时构建 x86 和 x64 PE DLL。主要卡点包括：

- WSL Meson 0.61 过旧，DXVK 子项目要求 Meson 1.x。
- WSL mingw-w64 8.0 缺少 `d3d11on12.h`。
- 把 llvm-mingw 新头文件注入旧 MinGW 后虽然编译通过，但链接出现 `nanosleep64` 未定义，证明头文件与运行库混用不可行。
- Windows llvm-mingw 的 `.exe` 不能直接复制到 WSL 执行，WSL 下载 release 资产又受网络限制。

最终改为 Windows 原生完整工具链：llvm-mingw UCRT、Meson、Ninja、Vulkan SDK/glslangValidator。x64 DLL 放入 `system32`，x86 DLL 放入 `syswow64`，生成 `dxvk-3.0.2.tzst`。核心经验是头文件、编译器、链接器和运行库必须来自同一套工具链。

### 6.4 VKD3D-Proton：复用已验证工具链

VKD3D-Proton 3.0.1 复用 DXVK 的 Windows llvm-mingw 流程，额外处理：

- 使用 llvm-mingw 自带 `widl` 编译 D3D12 IDL。
- 完整初始化 `dxil-spirv` 递归子模块。

最终完成 x86/x64 `d3d12.dll` 和 `d3d12core.dll`，按 `system32/syswow64` 结构封装并集成。该结果证明 DLL 类组件的工具链方案可以复用。

### 6.5 Turnip：复杂依赖链与 `dlopen` 缺库

内置 Turnip 经 `readelf` 确认为 glibc/X11 构建，不能用 Android NDK。构建过程配置 WSL arm64 sysroot、X11/XCB、flex/bison、pkg-config、glslang 15.1，并因 GCC 11 无法编译 Mesa 26.1 C++、Ubuntu 22.04 缺少合适交叉 GCC 13，最终改用：

```text
clang/clang++ --target=aarch64-linux-gnu
llvm-ar + llvm-strip + LLD
platforms=x11
vulkan-drivers=freedreno
freedreno-kmds=kgsl
```

首版真机报 `VK_ERROR_INCOMPATIBLE_DRIVER`。API、GLIBC、GLIBCXX 和 ICD 符号均正常；新旧 `NEEDED` 差分最终发现新版新增 `libxcb-xfixes.so.0`，rootfs 缺库导致 Vulkan loader 无法 `dlopen` 驱动。将约 30 KB arm64 依赖一并封入 Turnip 包后，D3D9、D3D11、D3D12 探针全部成功。

### 6.6 Wine 11：Android 平台耦合和最终根因

#### 6.6.1 wineserver 目录强绑定

通用 Wine 11 使用 `/tmp/.wine-<uid>`，但 Android 应用沙箱不能写根 `/tmp`，真机报 `Permission denied`。Wine client 和 server 分别构造服务目录，必须同时修改：

- `dlls/ntdll/unix/server.c`
- `server/request.c`

将路径统一改为 `<WINEPREFIX>/.wineserver`。第一次只改 client、或在旧打包后才补 server 都失败，形成两个重要约束：双侧补丁缺一不可；必须完成补丁、重编、`make install`、全量 strip 后再打包最终资产。

#### 6.6.2 WFM 假设的纠正

进入桌面后 `wfm.exe` 曾在 `0x14000268A` 空指针崩溃，早期判断为 Wine 11 移除了 WFM 所需 API。PE 导入表证明相关槽为静态 IAT，对应 API 在 Wine 10.10/11.0 均存在；同一 WFM 在 WSL 原生 Wine 11 可持续运行，因而原假设被推翻。

项目保留了 per-exe WFM 解释器策略和 `winefile` 兜底，用于降低风险和诊断，但它们不是最终 Wine 11 游戏故障的根因修复。

#### 6.6.3 版本二分与干扰项

围绕星露谷首帧前停滞建立了完整二分：

- Wine 10.10：稳定通过，形成基线。
- Wine 10.18：Android 缺少 `libEGL.so.1` 时未正确关闭 EGL 路径；回移上游 `eecf70a` 后通过。
- Wine 10.20：不完整 ICU stub 使 .NET FailFast；以 `WINEDLLOVERRIDES=icu=n` 隔离后通过。
- Wine 11.0-rc3、rc5：通过。
- Wine 11 正式版：0 次 swap，并出现两次 `c0000005`。
- `f11` 等长版本布局变体：通过，一度支持 ntdll/Box64 布局假设。
- Box64 0.4.5 和指定地址解释器对照：正式版仍失败，排除 dynarec 为直接根因。

#### 6.6.4 `nsiproxy` 最小修复

Box64 rolling log 与回溯将故障定位到 `nsiproxy.so+0x8533`：Android 权限环境中 `if_nameindex()` 返回 NULL，`RAX=0`，Wine 随后执行 `mov (%rax), %ebx`。强制解释器仍复现，证明是 Wine 缺少防御性检查，而不是翻译错误。

最终回移 Wine 上游 `08dbf01a`，只修改 `nsiproxy.sys:update_if_table()`：检查 NULL、记录 errno 并安全返回。补丁后仍可看到 `if_nameindex failed, errno 13`，但不再空指针解引用；游戏完成 GL context、`wglSwapBuffers` 和 `x11drv_surface_swap`。正式 Wine 11 分别在 Box64 0.4.3 与 0.4.5 下真机通过，0.4.5 还完成了无诊断变量的干净回归。

最终结论取代以下历史假设：WFM 缺失 API、WGL 交换实现回归、Box64 dynarec 或短版本布局是直接根因。

### 6.7 Wine 安装与容器链强化

项目2还完成了以下工程修复：

- Wine 安装器可向下展开最多 8 层单目录包装。
- Settings 中 Wine 管理入口不再依赖全局 `DEBUG_MODE`。
- 版本解析保留 rc/beta 后缀，避免 rc3、rc5 标识冲突。
- `winecfg` 后执行 `wineserver -k -w`，等待注册表完整写回。
- 压缩前校验 `system.reg/user.reg` 文件头和 `#arch=win64`。
- 压缩、复制、符号链接错误向上传播，失败时清理部分产物。
- 可选 Wine 使用自身 `common_dlls.json` 注入 32/64 位 DLL。
- 压缩前和容器启动前清理 `.wineserver` 运行时目录，避免模板权限 `0771` 触发 wineserver 安全退出。
- 为已验证 Wine 10.10 提供基于 ELF 指纹的事务式直装路径。

### 6.8 项目2验证结果

项目2最终标签为 `project2-wine11-final-20260804`。推荐 Wine addon：

```text
wine-11.0-final-nsiproxy-nullguard-winlator-custom-addon.tar.xz
SHA-256 A81595373E6C3FDAC8C33BEAE8BB1FDB7B7744DA63AF094E6BBECF2BD4A72765
```

项目2默认 Box64 0.4.5 候选 APK：

```text
SHA-256 DD06BB1676D1AEF1DA6E3CFD778CF4C716C8CEFA4C433398BA84CC545C02809D
```

项目2核心研发目标已完成。早期 Wine 升级文档顶部“回退 10.10”是历史阶段结论，最终状态以 Wine 11 执行记录 6.26-6.28 和完成验收摘要为准。

## 7. 项目3：游戏兼容性、输入与性能分析

### 7.1 Dave the Diver 启动问题闭环

初始日志证明 `DaveTheDiver.exe` 已创建，但使用未修复内置 Wine 11 时停在 `dnsapi/nsiproxy` 附近。切换项目2补丁 Wine 后越过第一停点，却进入新的 Unity/D3D 黑屏阶段。

之后按单变量依次验证：

1. `BOX64_UNITYPLAYER=1` 单独不能解决 DXVK 黑屏。
2. 一轮快捷方式参数出现重复引号，实际 EXE 未启动；修复 `Shortcut.java` 的可执行路径外层引号处理。
3. Unity `Player.log` 证明 DXVK 下已创建 D3D11 11.1 设备并完成输入初始化，但停在 Odin Serializer 后。
4. `-force-gfx-direct` 已生效，停点不变，排除多线程渲染为充分原因。
5. Box64 `Stability` 生效，停点不变，排除 Intermediate 内存策略为主因。
6. 只把 DX wrapper 切换为 WineD3D，游戏首次越过黑屏并进入 `Tutorial_Mission01`。
7. 移除 `-force-gfx-direct` 仍通过，说明关键变量是 WineD3D。
8. 从 Stability 恢复 Intermediate 仍通过，说明 Stability 非必要条件。
9. 删除 `BOX64_UNITYPLAYER=1` 后，两轮都在约 114-115 秒出现第二个非 `--attach` CrashHandler 并闪退。
10. 正确保存并恢复 `BOX64_UNITYPLAYER=1` 后，T1.10A2 越过重复闪退点；T1.10B 持续约 413 秒，完成第二次冷启动和 6 分钟验收。

当前样本的最小成功集为：

```text
Wine 11 nsiproxy NULL 保护
+ WineD3D
+ Box64 Intermediate
+ BOX64_UNITYPLAYER=1
```

不需要 `-force-gfx-direct` 或 Stability。由于测试游戏文件包含第三方替换层，该结论只覆盖已记录样本，不外推到所有官方副本。

### 7.2 修复集成到内置 rootfs

将项目2验证的 `nsiproxy.so` 集成进 `rootfs.tzst` 后，又遇到多层应用启动问题：

| 卡点 | 根因 | 修复 |
|---|---|---|
| APK 更新后仍运行旧模块 | `LATEST_VERSION` 未递增，手机不重新解压 rootfs | rootfs 版本升到 23，触发刷新 |
| 容器永久停在 `starting up` | rootfs 安装未完成也允许进入 Activity | 增加 rootfs 有效性/版本门槛，提示后退出 |
| guest 前静默失败无日志 | Java 后台阶段缺少独立观测；进程异常被吞 | 增加 `Documents/Winlator/startup.log` BEGIN/END/异常堆栈 |
| Box64 解压失败后不再重试 | 失败仍保存 `current_box64_version` | 校验文件存在且可执行后才保存版本；失败自动重试 |
| Box64 单文件 tar 解压失败 | 解压器写文件前没有创建 `usr/local/bin` 父目录 | `TarCompressorUtils.extract()` 统一创建父目录 |

T2-A4 真机首次成功；T2-B 同配置运行 6 分 51 秒并进入 `Tutorial_Mission01`，无 `c0000005`、SIGSEGV、Unhandled page fault 或 X 连接异常，内置修复闭环完成。

### 7.3 跨游戏触屏键盘修复

性能基线首轮暴露 Dave 与星露谷的触屏 `W/S/A/D` 同时无响应。跨游戏复现排除了单游戏配置，随后建立 Android 到 Windows 输入层的观测链：

```text
InputControls
  -> XServer/InputDeviceManager
  -> X11 KeyPress/KeyRelease + FocusIn/FocusOut
  -> WinHandler UDP 虚拟键对照
  -> Wine X11DRV_KeyEvent
  -> XKB keycode/keysym/scan
  -> Windows input/rawinput
```

主要排查过程：

- 新增 `InputEventLogger`，记录绑定、注入、去重、窗口 map/focus、订阅和丢弃原因。
- 日志证明 X11 焦点正常，press/release 已发送且 drop=0。
- 手动 `bringToFront()` 无效，排除 Win32 前台同步。
- WinHandler `VK_W` 双通道握手、排队、UDP `sent=true` 全部正常，仍无响应。
- 实现标准 X11 `FocusIn/FocusOut` 后，Wine 窗口收到通知但仍无响应。
- 自动捕获 Wine trace 后，确认 `X11DRV_KeyEvent` 收到 `keysym=a`、`vkey=0x41`，但扫描码为备用值 `0x78`。
- 修复 `GetKeyboardMapping` 的数组容量、reply length、起始索引和写入数量后，映射回复完整，Wine 仍报告 keycode 38 为 `NoSymbol`。
- 最终确认 Wine 通过 XKB API 读取布局，而当前 Java XServer 没有完整 XKEYBOARD 扩展。

解决方案是增加 3,024 bytes 的 x86_64 `libwinlator-xkb-core.so`，只实现运行时需要的 `XkbKeycodeToKeysym` 和 `XkbTranslateKeySym`，通过 `BOX64_LD_PRELOAD` 注入 guest，回退到已有 X11 核心映射。

真机结果：`A` 从 `scan 0x78` 恢复为标准 `0x1e`，`W/S/D` 分别恢复标准扫描码；星露谷和 Dave 都确认实际响应，Dave 日志出现 Raw Input 读取。该修复不要求修改游戏文件或完整实现 XKB wire protocol。

### 7.4 性能采样工具与当前缺口

`FrameRating` 已扩展为 CSV 性能采样器，支持：

- 唯一文件名和碰撞后缀，不覆盖旧样本。
- `elapsed_ms`、`frame_time_ms`、窗口 FPS、RAM 使用量、CPU 最大频率。
- 60 秒预热、180 秒测量。
- 窗口 ID、名称、class、surface 状态和窗口切换分段。
- 平均 FPS、1% Low、平均帧时间和 P99 帧时间摘要。
- FPS 面板长按重置测量分段。

首份 CSV 只有表头，原因是旧选择逻辑只接受 surface 窗口，而 WineD3D 应用窗口为 `surface=false`。G3-P1 扩展了非 surface 应用窗口选择，但 G3-P2 真机约 15 分钟会话仍只记录两次 Dave 更新，且导出时容器未销毁，没有 `session_end`。这说明已选中的 application window 不是持续更新的实际 renderable 子窗口。

因此当前只能确认“性能采样框架已实现，窗口选择向前推进”，不能给出平均 FPS、1% Low 或优化前后对比。项目3原始任务3仍未完成，后续需要把采样目标继续下沉到真实更新的 drawable/renderable 子窗口。

### 7.5 HUD 鼠标回归

G3-P1 把长按监听注册在全屏 `FrameRating` 根视图，使该层变成 long-clickable 并覆盖 `TouchpadView`，导致 HUD 外拖动也被提前消费，用户看到鼠标无法移动。

G3-P2 只把长按监听移动到可见的 `LLFPSPanel`，不修改 Touchpad、XServer、Wine 或 XKB。真机回归中鼠标恢复、触屏按键有效；日志记录 48 次 press、48 次 release 和 58 次 pointer focus。长按无振动与立即 flush 仍属于性能工具交互问题，不阻塞兼容性测试。

### 7.6 多游戏测试矩阵

项目3任务1采用统一基线：`1280x720`、Turnip/Zink、Box64 Intermediate、ALSA、HUD Simple，并使用结果分类与单变量对照。

| 游戏 | 有效结论 | 关键技术证据 | 当前处理 |
|---|---|---|---|
| Dave the Diver | 有效通过 | WineD3D + UnityPlayer 1；双冷启动；6 分钟稳定；输入有效 | 兼容性闭环完成 |
| Stardew Valley | 有效通过 | Wine 11 NULL 保护后出帧；XKB 修复后键盘有效 | 作为 Wine/输入交叉验证样本 |
| Escape from Duckov | `FAIL-BLACK` | DXVK/WineD3D 均创建 D3D11 11.1；补 `E:\Saves` 后目录异常消失，但 Easy Save 仍为空备份路径异常 | 归为当前包保存配置兼容候选，不改 Winlator 公共图形层 |
| 合金装备5：原爆点 | `FAIL-NO-WINDOW` / `FAIL-CRASH + PACKAGE-SCOPED` | 单次用户操作仍产生两个相隔 6 秒实例；快捷方式绕过 WFM后仍退出；WineD3D 推进更深但未解决 | 已停止扩大变量 |
| Sifu | 尚无有效结果 | 两轮均未启动 `Sifu-Win64-Shipping.exe` | 待正确快捷方式重新测试 |
| Palworld | 未测试 | 用户当前无法提供测试条件 | 移出本轮分母 |

#### Duckov A-D 对照

- A：DXVK 黑屏，无明确崩溃。
- B：只切 WineD3D；Unity 已完成 D3D11、输入、触摸、PhysX 初始化，定位到 `E:\Saves` 缺失。
- C：只创建空 `E:\Saves`；`DirectoryNotFoundException` 消失，但 Easy Save 备份路径为空。
- D：保留 Saves，只回切 DXVK；仍停在相同 Easy Save 异常。

该矩阵表明保存/选项配置是当前包体的直接兼容候选，但不是黑屏的充分解释；没有证据支持修改 Winlator 公共图形、Wine 或 Box64。

#### 原爆点 A-D 对照

- A：观察不足 5 分钟且出现双进程，仅作诊断。
- B：确认用户只双击一次，仍产生两个相隔 6 秒的进程；观察 7 分 23 秒无窗口，构成有效 `FAIL-NO-WINDOW`。
- C：首页快捷方式绕过 WFM，双进程和快速退出仍存在，分类 `FAIL-CRASH`。
- D：只切 WineD3D，图形枚举推进更深但仍退出；证明问题不专属于 DXVK/Vulkan。

当前包含第三方 Steam 模拟层，所有结论必须带 `PACKAGE-SCOPED`，不能外推到官方发行版。

### 7.7 Sifu 当前状态

静态检查确认真正游戏程序为：

```text
E:\SIFU Deluxe Edition\Sifu\Binaries\Win64\Sifu-Win64-Shipping.exe
```

两轮用户可见“闪退”均为无效测试：

1. 2026-08-14：实际仍在容器 10 启动 `MgsGroundZeroes.exe`，只额外加入 `-dx11`。
2. 2026-08-24：容器 1 启动完成，但 argv 指向游戏根目录 `Sifu.exe`，工作目录为 `E:\SIFU Deluxe Edition`；没有 Shipping 进程、游戏窗口、图形设备、退出码或崩溃回溯。

第二轮还出现 `libdbus`、`libudev`、`winebus` 错误，但在真实游戏进程未出现前不能认定为 Sifu 根因。当前分类保持 `INVALID-WRONG-SHORTCUT`，不计入覆盖、不标记 `FAIL-CRASH`。

有效下一轮只能改变快捷方式入口：独立容器、Shipping EXE、正确 Win64 工作目录、参数 `-dx11`；其余基线保持不变。项目3现已暂停，该轮尚未执行。

### 7.8 构建环境恢复与真机交付

2026-08-23 本机 Gradle 首次因缺少 SDK 路径失败。后续确认 SDK 位于 `C:\Users\Wanna\AppData\Local\Android\Sdk`，API 34、Build Tools 34.0.0、CMake 3.22.1 可用，但项目要求 NDK 24，机器最初只有 NDK 22。

Gradle 自动安装 NDK 返回 `Archive is not a ZIP archive`，因此手工下载并校验官方 NDK r24：

```text
android-ndk-r24-windows.zip
663,076,813 bytes
SHA-1 75f9c281c64762d18c84da465f486c60def47829
```

安装后 `:app:assembleDebug` 用时 2 分 21 秒并成功。当前真机交付 APK：

```text
D:\study\Winlator\delivery\Winlator-wzh-11.1-debug-arm64-v8a-20260824.apk
206,825,411 bytes
SHA-256 DCE55D579B2A1338C72F4A3A60A1496CEF9598BC7B11EBDCBF3A07A3601BB584
```

门禁结果：包名/版本/ABI 正确，APK V2 签名通过，`zipalign -c -v 4` 通过，74 个 assets 条目与构建资产聚合哈希通过。APK 保存在仓库外，不进入 Git；用户采用 USB/MTP 手动传输，不依赖 adb。

## 8. 当前关键资产与哈希

| 资产 | 大小 | SHA-256 |
|---|---:|---|
| `assets/rootfs.tzst` | 79,289,327 | `2419CAD38D3C3992827151CD7D46C18E19085375403D01C5478A1F9EC4D97CBD` |
| `assets/container_pattern.tzst` | 25,173,966 | `E608C8A1F89F34D733A46EAFCB5C4630BFDE35809E46BA5EF843DB3FED0CA22A` |
| Box64 0.4.5-dev | 4,860,076 | `1F9AD71A608FD1FC7F4AA22B667354D66237FAB870E72F9E07CF83E892E2A083` |
| Box64 0.4.3 | 4,735,675 | `72D01F276B3EDBA9B19536452E6F12BB859FBC04464E89C42DCC7E57D63B5DB8` |
| DXVK 3.0.2 | 8,966,523 | `0C2940EF830DDA63597349DD1BF069926C79E8C6FE70A7FCB337313EF4067B95` |
| VKD3D 3.0.1 | 3,489,876 | `84233675CAACC8CC910EF84C1FD8038BC3689D279B24911262473E28BED012C1` |
| Turnip 26.1.5 | 1,883,377 | `3B1D3DE89BC1328C92BFFF84B9D2FFC8EC388EC6D6AAC30F81E74BDC8655B1CB` |
| XKB fallback | 3,024 | `B2D9ECB6CB0F05895C59FACCB195BE1641029A185C9371CFC1B666F955D83F4E` |

## 9. 研发证据、脚本和工作方法

### 9.1 证据规模

本报告编写前的仓库归档包括：

- 87 个直接运行/输入/性能日志，约 16.7 MB。
- 116 个组件和 Android 构建日志，约 21.1 MB。
- 10 个增量日志导入快照，共 88 个快照文件，约 10.7 MB。
- 12 个可复现实验组件包/哈希文件，约 555 MB。
- 11 个图形或配置测试文件。
- 4 个游戏目录/包体静态清单。
- 20 份项目阶段文档（本报告和本轮计划写入前）。

快照中可能引用或复制相同内容，因此文件数不等同于唯一实验轮次；唯一性以 SHA-256 和 `MANIFEST.csv` 为准。

### 9.2 日志归档脚本

`scripts/archive-development-logs.ps1` 负责：

- 扫描工作区 `logs/`、`build-logs/`、`test/`。
- 使用 Git blob 和 SHA-256 识别远程/本地已归档内容。
- 新内容写入唯一 `archive/log-imports/<SnapshotId>/`。
- 生成来源、大小、哈希、状态和归档位置清单。
- 对可能凭据执行正则扫描。
- 对单文件执行 95 MiB 上限门禁。

`scripts/create-game-inventory.ps1` 只记录游戏目录文件数、总字节和非分发状态，不复制商业内容。

### 9.3 实验方法

整个项目逐步形成以下稳定方法：

1. 先建立可运行或可复现失败基线。
2. 通过静态检查确认实际版本、架构、入口和动态依赖。
3. 把观测点推进到故障发生层，而不是用用户可见现象猜根因。
4. 每轮只改变一个兼容变量，并从日志确认变量真正生效。
5. 对成功与失败样本同时保留时长、进程、调用层和哈希。
6. 修复采用最小范围，并做反向回切或第二版本交叉验证。
7. 所有阶段性结论、日志和计划同步提交到 `origin/main`。

## 10. 典型卡点总览

| 卡点 | 最终根因 | 解决方式 | 方法沉淀 |
|---|---|---|---|
| Box64 首版 `bad ELF magic` | Bionic 目标与 glibc rootfs 不匹配 | 改用 `aarch64-linux-gnu`/`WINLATOR_GLIBC` | 编译前先判定目标 libc/ABI |
| Box64 main 前 SIGSEGV | `set-rpath`/strip 破坏固定地址非 PIE ELF | 只设 interpreter，不改 rpath、不 strip | 无早期日志优先查加载/打包层 |
| DXVK 链接 `nanosleep64` | 新头文件与旧 MinGW 运行库混用 | Windows 原生完整 llvm-mingw 工具链 | 头、编译器、库必须自洽 |
| Turnip `INCOMPATIBLE_DRIVER` | rootfs 缺少新增 `libxcb-xfixes.so.0` | 依赖随驱动包一起分发 | 比较新旧 `NEEDED` 差集 |
| Wine `/tmp` 权限失败 | 通用 wineserver 路径不适配 Android 沙箱 | ntdll/server 双侧改到 WINEPREFIX | 运行时环境假设也是 ABI 的一部分 |
| Wine 11 正式版首帧前崩溃 | `if_nameindex()` NULL 后 `nsiproxy` 未判空 | 回移上游 `08dbf01a` 最小补丁 | 版本二分 + 栈定位 + 解释器反证 |
| 可选 Wine prefix 损坏 | 未等待 wineserver 写回、无 registry 校验 | `-k -w`、格式校验、事务压缩/移动 | 模板生成必须具备完成性门禁 |
| Dave DXVK 黑屏 | 当前样本 DXVK 路径无法越过 Unity 停点 | 单变量切到 WineD3D | 图形设备创建成功不等于应用可见成功 |
| Dave 约 115 秒闪退 | `BOX64_UNITYPLAYER=0` 不适合当前样本 | 快捷方式保持 `BOX64_UNITYPLAYER=1` | 以失败重复和成功回切证明因果 |
| 容器永久 `starting up` | rootfs 缓存、Java 静默阶段、Box64 解压父目录问题叠加 | 版本门槛、startup.log、重试和父目录修复 | 启动链需分阶段可观测和自愈 |
| 触屏键盘无响应 | XKB 得到 `NoSymbol`，扫描码退化为 `0x78` | 核心映射修复 + x86_64 XKB fallback | 沿输入链逐层排除而非只看按钮 |
| HUD 导致鼠标失效 | 全屏 long-clickable View 截获触摸 | 监听范围缩到 FPS 面板 | 诊断工具也必须做交互回归 |
| Duckov 黑屏 | 当前包保存/选项路径异常，但不足以解释全部黑屏 | A-D 单变量对照后停止扩大变量 | 没有公共层证据时不修改框架代码 |
| Sifu “闪退” | 实际快捷方式没有启动 Shipping EXE | 标记无效并要求正确入口重测 | 先验证测试对象，再分类兼容结果 |

## 11. 当前未完成事项与风险

### 11.1 项目3任务1：Sifu 有效测试

当前多游戏覆盖为 4/5。Sifu 需要在独立容器中从 `Sifu-Win64-Shipping.exe` 建立快捷方式并导出完整日志。两轮历史操作都属于错误入口，不能形成兼容性结论。项目3当前暂停，因此该项保持未完成。

### 11.2 项目3任务3：性能优化

采样器功能和 CSV 格式已实现，但真实持续渲染子窗口尚未被选中，现有 CSV 不能计算可信 FPS/1% Low。后续应：

1. 将采样目标下沉到持续触发 Drawable 更新的 renderable 子窗口。
2. 确保手动重置与 Activity 结束立即 flush，并记录 `session_end`。
3. 在 Dave `Tutorial_Mission01` 固定路线完成 60 秒预热 + 180 秒基线。
4. 依次单变量比较 Strict Shader Math、Box64 Performance、`960x544`，记录平均 FPS、1% Low、P99 帧时间、稳定性和图形正确性。
5. 只有取得有效基线和优化后对照，才能完成“渲染转换效率提升方案并实现”的原始要求。

### 11.3 样本外推风险

- Dave、Duckov、原爆点、Sifu 的部分目录存在第三方修改或 Steam 模拟层；结论必须绑定当前指纹。
- 原爆点与 Sifu 必须使用 `PACKAGE-SCOPED`，不能当作官方发行版公共缺陷。
- 项目1构建使用宽范围 `pickFirst('**/*.so')`，后续可收窄到具体冲突库。
- XKB fallback 是针对当前 Wine/XServer 缺口的窄兼容层，不等价于实现完整 XKEYBOARD 协议。
- Wine 11 的网络接口 NULL 保护阻止崩溃，但 Android `errno 13` 条件仍存在，依赖真实网络接口枚举的软件可能有功能降级。

## 12. 建议的恢复顺序

项目3恢复后建议严格按以下顺序继续：

1. 修正 Sifu 快捷方式，完成有效 T1-SIFU-A 并形成五游戏最终矩阵。
2. 汇总项目3任务1最终报告，区分 PASS、FAIL、PACKAGE-SCOPED 和无效轮次。
3. 修正 FrameRating 实际更新窗口选择，取得第一份有效 Dave 性能基线。
4. 执行单变量性能优化矩阵并完成前后对比。
5. 根据结果决定优化是否进入默认配置；若只适用于单游戏，则保留为快捷方式级兼容配置。

## 13. 关键提交与文档索引

### 13.1 关键提交

- `a217bdb`：项目1桌面关机悬浮球。
- `129cb83`：项目2技术架构与调用链。
- `6e5e14f`：Box64 正确升级到 0.4.3。
- `e5e118e`：DXVK 3.0.2。
- `49c2d79`：VKD3D 3.0.1。
- `d7099bf`：Turnip 26.1.5。
- `1d4585d`：Wine 11 最终修复、Box64 0.4.5 和研究材料归档。
- `b429965`：Dave 双冷启动验收。
- `b12ff46`：将 Wine `nsiproxy` 修复集成进内置 rootfs。
- `401ec28`：递增 rootfs 更新门槛，确保手机刷新资产。
- `5427deb`：增加容器启动阶段日志和 Box64 解压重试。
- `226d0cd`：修复压缩包文件父目录缺失。
- `028c0a1`：内置修复 6 分钟回归。
- `3cd8ceb`：XKB 核心映射兼容层。
- `b3115a2`：HUD 鼠标回归修复与任务1计划。
- `cf1ff30`：Duckov A-D 对照收口。
- `bd750d4`：原爆点 WineD3D 对照收口。
- `5bf6261`：2026-08-24 Sifu 无效快捷方式日志归档。

### 13.2 主要文档

- [`docs/项目1-桌面悬浮球关机功能.md`](docs/项目1-桌面悬浮球关机功能.md)
- [`docs/项目2-技术架构与调用路径.md`](docs/项目2-技术架构与调用路径.md)
- [`docs/项目2-box64编译升级.md`](docs/项目2-box64编译升级.md)
- [`docs/项目2-DXVK编译升级.md`](docs/项目2-DXVK编译升级.md)
- [`docs/项目2-VKD3D编译升级.md`](docs/项目2-VKD3D编译升级.md)
- [`docs/项目2-turnip编译升级.md`](docs/项目2-turnip编译升级.md)
- [`docs/项目2-Wine11-WFM攻坚执行记录.md`](docs/项目2-Wine11-WFM攻坚执行记录.md)
- [`docs/项目2-完成验收与归档.md`](docs/项目2-完成验收与归档.md)
- [`docs/项目3-Dave-the-Diver启动故障修复计划与执行记录.md`](docs/项目3-Dave-the-Diver启动故障修复计划与执行记录.md)
- [`docs/项目3-任务1-多游戏运行测试计划与执行记录.md`](docs/项目3-任务1-多游戏运行测试计划与执行记录.md)
- [`docs/项目3-任务1-Sifu闪退日志分析计划-20260824.md`](docs/项目3-任务1-Sifu闪退日志分析计划-20260824.md)
- [`docs/项目2-项目3典型卡点总结与PPT计划-20260828.md`](docs/项目2-项目3典型卡点总结与PPT计划-20260828.md)
- [`docs/仓库与原版差异基线-20260823.md`](docs/仓库与原版差异基线-20260823.md)

## 14. 最终结论

本练习已经从单一 UI 功能扩展为一套较完整的 Winlator 平台优化实践：完成桌面交互增强，建立组件交叉编译与资产集成方法，解决 Android 沙箱下 Wine 11 的关键兼容问题，形成 Box64/Wine/图形链的可复现版本基线，并通过 Dave、星露谷及多款失败样本建立了日志驱动的兼容性分析流程。

当前最大的剩余工作不是继续无序调整兼容参数，而是完成两个严格受控的闭环：正确入口下的 Sifu 有效首轮，以及能真实捕获持续帧更新的性能基线。完成这两项后，项目3才能形成完整五游戏矩阵和有数据支撑的性能优化结论。
