# `winlator_wzh` 与原版 Winlator 差异基线（2026-08-23）

## 复核范围

- 练习仓库：`D:\study\Winlator\winlator_wzh`，`main`/`origin/main`=`987d321`。
- 原版仓库：`D:\study\Winlator\winlator`，`main`/`origin/main`=`5949297`。
- 练习说明与归档副本 SHA-256：`9D671193ECAA6B46874743F21559410CF1E023812A02F1C7BD2C1D9D8921ABB4`。
- 原版仓库的 `app`、`gladio`、`vortek` 子模块在本地未初始化，原版工作树只有顶层仓库和子模块指针；因此代码级基线以练习仓库的初始上游快照 `5acd6bc`（upstream `e113da42`）为基准，同时记录原版当前组件指针，避免把“本地子模块为空”误判为源码删除。

## 仓库规模与组织

| 项目 | `winlator_wzh` | 原版工作树 |
|---|---:|---:|
| 顶层远程 | `SilenceWanna/winlator_wzh` | `brunodev85/winlator` |
| 顶层提交 | `987d321` | `5949297` |
| tracked 文件（约） | 1,355 | 150 |
| Java/C/C++/资源 | 完整 vendored 源码 | `app` 子模块未展开 |
| 组件资产 | 内置 Box64/Wine/DXVK/VKD3D/Turnip 与 rootfs | 主要为 `installable_components`、`wine_addons` |
| 研究记录 | `docs/`、`archive/`、日志清单和哈希 | 无本练习记录 |

## 相对初始上游的代码改动

不含归档、二进制资产和文档时，当前 fork 相对 `5acd6bc` 共涉及约 53 个文本/源码路径，核心改动可分为：

1. 项目 1：新增 `ShutdownBallView`、布局/背景资源，并在 `XServerDisplayActivity` 接入可拖动关机悬浮球、点击确认和位置持久化。
2. 项目 2：升级默认组件和版本门槛；增加 Wine 11/rootfs 兼容、Wine 安装校验、压缩包解压父目录创建、Box64 重试和启动阶段错误收口；保留 Wine 10.10 回退。
3. 项目 3 启动与输入诊断：增加 `startup.log` 阶段日志、`InputEventLogger`、X11 FocusIn/FocusOut 通知、键盘映射修复、x86_64 XKB fallback preload、Unity/WinHandler 观测和 HUD/帧率采样器。
4. 任务记录与复现：增加 `AGENTS.md`、日志归档脚本、游戏清单脚本、Wine 补丁、完整构建/运行/输入证据和项目 3 多游戏测试记录。

## 组件与行为基线

| 层 | 当前 fork 基线 | 相对原版/上游的意义 |
|---|---|---|
| Box64 | 内置 `0.4.5-dev-372739d`，保留 `0.4.3` | 默认二进制翻译器升级，`Intermediate` 为任务 1 公共配置 |
| Wine | 内置 Wine 11.0，保留 Wine 10.10；`nsiproxy` `if_nameindex()` NULL 保护 | 修复 Android `errno=13` 网络接口查询导致的启动阻塞 |
| DXVK | 3.0.2，保留旧版本 | D3D11 → Vulkan 包装层升级 |
| VKD3D | 3.0.1 | D3D12 → Vulkan 包装层升级 |
| Turnip | 26.1.5 | Adreno 图形驱动升级 |
| 输入 | X11 键盘映射修复 + XKB fallback + focus 通知 | 任务 1 前已用 Dave/星露谷真机回归证明鼠标与按键可用 |
| 启动 | rootfs 版本门槛、阶段日志、Box64 重试和错误提示 | 防止未完成安装进入 `starting up` 后静默失败 |

## 任务 1 当前结论

- Dave the Diver、Stardew Valley：有效真机启动、交互和稳定性证据已归档。
- Escape from Duckov：A-D 均 `FAIL-BLACK`；DXVK/WineD3D 均完成 D3D11 初始化，补齐 `E:\Saves` 后仍在 Easy Save 空备份路径异常，结论限当前包体。
- 合金装备 5：原爆点：B 为 `FAIL-NO-WINDOW + PACKAGE-SCOPED`，C/D 为 `FAIL-CRASH + PACKAGE-SCOPED`，已停止扩大变量。
- Palworld：当前无法测试，不计入分母。
- 师父：A0 误选容器 10 的原爆点快捷方式，`INVALID-WRONG-SHORTCUT`；尚无师父进程证据。

所以任务 1 仍为 4/5 有效覆盖，下一最小单元是独立容器中的 `Sifu-Win64-Shipping.exe` 首轮测试；没有真机导出日志前，不得标记通过、失败或完成。

## 基线限制

原版 `app`/`gladio`/`vortek` 子模块未初始化，无法在不下载外部源码的情况下对其每一行做当前版本 diff；这不影响初始上游快照和 fork 提交历史对项目 1-3 改动的审计。若后续需要对原版当前子模块逐文件复核，应在独立临时目录初始化，不改写用户原版工作树。
